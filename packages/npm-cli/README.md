# PathStash npm CLI

This package provides a TypeScript/Node CLI for developers who prefer:

```sh
npx pathstash signup --email you@example.com
bunx pathstash init --root .
```

The Rust CLI remains the primary release target for the deepest filesystem support. The npm CLI covers account setup, workspace initialization, scanning, pushing selected files, and device/account checks with no native install step.
